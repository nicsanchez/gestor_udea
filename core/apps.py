from django.apps import AppConfig

# Configuración del la aplicación core, usada para las vistas home, base, login y añadir noticia.
class CoreConfig(AppConfig):
    name = 'core'
    verbose_name = 'Principal'

